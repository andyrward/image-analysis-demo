[project]
name = "image-analysis-demo"
version = "0.1.0"
description = "Image analysis demo with spot generation"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "numpy>=2.0.2",
    "pillow>=11.3.0",
    "jupyter>=1.0.0",
    "matplotlib>=3.8.0",
]
